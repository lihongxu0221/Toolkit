<UserControl x:Class="BgCommon.Script.Views.ScriptDocumentView"             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              xmlns:editor="clr-namespace:RoslynPad.Editor;assembly=RoslynPad.Editor.Windows"             xmlns:rp="clr-namespace:RoslynPad"             xmlns:v="clr-namespace:BgCommon.Script.Views"             xmlns:vm="clr-namespace:BgCommon.Script.ViewModels"             mc:Ignorable="d"              d:DataContext="{d:DesignInstance vm:OpenDocumentViewModel}">    <UserControl.Resources>        <ResourceDictionary>            <ResourceDictionary.MergedDictionaries>                <ResourceDictionary Source="/BgCommon.Script;component/Assets/Style/General.xaml" />            </ResourceDictionary.MergedDictionaries>        </ResourceDictionary>    </UserControl.Resources>    <FrameworkElement.InputBindings>        <KeyBinding Gesture="F5" Command="{Binding RunCommand, Mode=OneTime}" />        <KeyBinding Gesture="F2" Command="{Binding RenameSymbolCommand, Mode=OneTime}" />        <KeyBinding Gesture="Shift+F5" Command="{Binding TerminateCommand, Mode=OneTime}" />        <KeyBinding Gesture="Ctrl+S" Command="{Binding SaveCommand, Mode=OneTime}" />        <KeyBinding Gesture="Ctrl+D" Command="{Binding FormatDocumentCommand, Mode=OneTime}" />        <KeyBinding Gesture="Ctrl+K" Command="{Binding CommentSelectionCommand, Mode=OneTime}" />        <KeyBinding Gesture="Ctrl+U" Command="{Binding UncommentSelectionCommand, Mode=OneTime}" />    </FrameworkElement.InputBindings>    <DockPanel>        <StatusBar DockPanel.Dock="Bottom"                   Background="{DynamicResource {x:Static rp:ThemeDictionary.StatusBarBackground}}"                   Foreground="{DynamicResource {x:Static rp:ThemeDictionary.StatusBarForeground}}">            <StatusBar.ItemsPanel>                <ItemsPanelTemplate>                    <DockPanel LastChildFill="False" />                </ItemsPanelTemplate>            </StatusBar.ItemsPanel>            <StatusBarItem>                <TextBlock>                    Ln <Run Name="Ln" />                    Col <Run Name="Col" />                </TextBlock>            </StatusBarItem>        </StatusBar>        <Grid>            <Grid.RowDefinitions>                <RowDefinition Height="Auto" />                <RowDefinition />            </Grid.RowDefinitions>            <ToolBar Grid.ColumnSpan="3"                     Background="Transparent"                     BorderBrush="{DynamicResource {x:Static rp:ThemeDictionary.TabBarBorder}}"                     BorderThickness="0 0 0 1">                <Button Command="{Binding RunCommand}" ToolTip="Run (F5)">                    <Grid>                        <Image>                            <Image.Style>                                <Style TargetType="Image">                                    <Setter Property="Source"                                            Value="{StaticResource Run}" />                                    <Style.Triggers>                                        <DataTrigger Binding="{Binding IsLiveMode}"                                                     Value="True">                                            <Setter Property="Source"                                                    Value="{StaticResource Rocket}" />                                        </DataTrigger>                                    </Style.Triggers>                                </Style>                            </Image.Style>                        </Image>                        <ProgressBar VerticalAlignment="Bottom"                                     Height="4"                                     IsIndeterminate="{Binding IsRunning}"                                     BorderThickness="0"                                     Visibility="{Binding IsRunning, Converter={StaticResource Boolean2VisibilityConverter}}" />                    </Grid>                </Button>                <Menu Background="Transparent" Visibility="Collapsed">                    <MenuItem Header="▾"                              Padding="0"                              FontFamily="Segoe UI Symbol">                        <MenuItem Command="{Binding ToggleLiveModeCommand}" Icon="{StaticResource Rocket}">                            <MenuItem.Style>                                <Style TargetType="MenuItem">                                    <Setter Property="Header"                                            Value="Live Mode" />                                    <Style.Triggers>                                        <DataTrigger Binding="{Binding IsLiveMode}"                                                     Value="True">                                            <Setter Property="Header"                                                    Value="Turn off Live Mode" />                                        </DataTrigger>                                    </Style.Triggers>                                </Style>                            </MenuItem.Style>                        </MenuItem>                    </MenuItem>                </Menu>                <Grid Visibility="{Binding HasReportedProgress, Converter={StaticResource Boolean2VisibilityConverter}}"                      Width="60"                      Margin="5, 0">                    <ProgressBar Value="{Binding ReportedProgress, Mode=OneWay}"                                 Minimum="0.0"                                 Maximum="1.0" />                    <TextBlock Text="{Binding ReportedProgress, Mode=OneWay, Converter={StaticResource DoubleToPercentageTextConverter}}"                               HorizontalAlignment="Center"                               VerticalAlignment="Center" />                </Grid>                <Button Command="{Binding TerminateCommand, Mode=OneTime}" ToolTip="Terminate process (Shift+F5)" Content="{StaticResource Stop}" IsEnabled="{Binding IsRunning}" />                <!--<StackPanel Orientation="Horizontal" Visibility="Collapsed">                    <ComboBox ItemsSource="{Binding AvailablePlatforms, Mode=OneTime}" SelectedItem="{Binding Platform}" HorizontalContentAlignment="Stretch" />                    <Button Content="{StaticResource SetPlatform}" ToolTip="Set platform as default" Command="{Binding SetDefaultPlatformCommand, Mode=OneTime}"  />                </StackPanel>                <Separator />-->                <Button Command="{Binding SaveCommand, Mode=OneTime}" ToolTip="Save (Ctrl+S)" Content="{StaticResource Save}" />                <Button Command="{Binding OpenBuildPathCommand, Mode=OneTime}" ToolTip="Open Build Folder" Content="{StaticResource PackageFolder}" />                <Separator />                <Button Content="{StaticResource FormatDocument}" ToolTip="Format Document (Ctrl+D)" Command="{Binding FormatDocumentCommand, Mode=OneTime}" />                <Button Content="{StaticResource CommentCode}" ToolTip="Comment Selection (Ctrl+K)" Command="{Binding CommentSelectionCommand, Mode=OneTime}" />                <Button Content="{StaticResource UncommentCode}" ToolTip="Uncomment Selection (Ctrl+U)" Command="{Binding UncommentSelectionCommand, Mode=OneTime}" />                <Button Content="{StaticResource Rename}" ToolTip="Rename Symbol (F2)" Command="{Binding RenameSymbolCommand, Mode=OneTime}" />            </ToolBar>            <editor:RoslynCodeEditor x:Name="Editor"                                     x:FieldModifier="private"                                     ContextActionsIcon="{StaticResource Bulb}"                                     Background="{DynamicResource {x:Static rp:ThemeDictionary.EditorBackground}}"                                     Foreground="{DynamicResource {x:Static rp:ThemeDictionary.EditorForeground}}"                                     FontFamily="{Binding MainViewModel.Settings.EditorFontFamily}"                                     Grid.Row="1"                                     Grid.Column="0"                                     Loaded="Editor_OnLoaded" />        </Grid>    </DockPanel></UserControl>