<UserControl x:Class="BgCommon.Prism.Wpf.Modules.Common.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:bg="http://www.sz-baigu.com/"
             xmlns:v="clr-namespace:BgCommon.Prism.Wpf.Modules.Common.Views"
             xmlns:vm="clr-namespace:BgCommon.Prism.Wpf.Modules.Common.ViewModels"
             xmlns:markup="clr-namespace:BgCommon.Prism.Wpf.Localization.Markup"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ChangePasswordViewModel}"
             d:DesignHeight="360" d:DesignWidth="480" Height="360" Width="480" >
    <Grid Margin="50 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <PasswordBox  hc:InfoElement.TitleWidth="120"
                          hc:InfoElement.TitlePlacement="Top"
                          hc:InfoElement.ShowClearButton="True"
                          hc:InfoElement.Title="{markup:StringResource 密码,StringFormat={}{0}：}"
                          hc:TitleElement.VerticalAlignment="Center"
                          hc:TitleElement.HorizontalAlignment="Left"
                          hc:InfoElement.ContentHeight="42"
                          Style="{DynamicResource PasswordBoxExtend}"
                          bg:TouchKeyboard.IsEnabled="True"
                          bg:PasswordHelper.Attach="True"
                          bg:PasswordHelper.Password="{Binding Password,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                          Margin="0 15 0 0"/>
            <PasswordBox  hc:InfoElement.TitleWidth="120"
                          hc:InfoElement.TitlePlacement="Top"
                          hc:InfoElement.ShowClearButton="True"
                          hc:InfoElement.Title="{markup:StringResource 确认密码,StringFormat={}{0}：}"
                          hc:TitleElement.VerticalAlignment="Center"
                          hc:TitleElement.HorizontalAlignment="Left"
                          hc:InfoElement.ContentHeight="42"
                          Style="{DynamicResource PasswordBoxExtend}"
                          bg:TouchKeyboard.IsEnabled="True"
                          bg:PasswordHelper.Attach="True"
                          bg:PasswordHelper.Password="{Binding ConfirmPassword,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                          Margin="0 15 0 0"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0 0 0 30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" 
                    Style="{DynamicResource ButtonPrimary}"
                    Content="{markup:StringResource 确认}"
                    HorizontalAlignment="Stretch"
                    Width="Auto" Height="40"  
                    Command="{Binding OkCommand}"
                    IsDefault="True"/>
            <Button Grid.Column="1" 
                    Style="{DynamicResource ButtonBaseStyle}"
                    Content="{markup:StringResource 取消}"
                    HorizontalAlignment="Stretch"
                    Width="Auto" Height="40"  
                    Command="{Binding CancelCommand}"
                    IsCancel="True" Margin="15 0 0 0"/>
        </Grid>
    </Grid>
</UserControl>
