<UserControl x:Class="BgCommon.Prism.Wpf.Authority.Modules.User.Views.UserManagementView"             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"             xmlns:prism="http://prismlibrary.com/"             xmlns:hc="https://handyorg.github.io/handycontrol"             xmlns:bg="http://www.sz-baigu.com/"             xmlns:local="clr-namespace:BgCommon.Prism.Wpf.Authority"             xmlns:view="clr-namespace:BgCommon.Prism.Wpf.Authority.Modules.User.Views"             xmlns:vm="clr-namespace:BgCommon.Prism.Wpf.Authority.Modules.User.ViewModels"             xmlns:sys="clr-namespace:System;assembly=mscorlib"             mc:Ignorable="d" d:DesignHeight="680" d:DesignWidth="1024"                     d:DataContext="{d:DesignInstance vm:UserManagementViewModel}"             prism:ViewModelLocator.AutoWireViewModel="True"             FontSize="{DynamicResource SubHeadFontSize}"             Foreground="{DynamicResource PrimaryTextBrush}"             Background="Transparent">    <UserControl.Resources>        <ResourceDictionary>            <ResourceDictionary.MergedDictionaries>                <ResourceDictionary Source="pack://application:,,,/BgCommon.Prism.Wpf;component/Assets/Style/Theme.xaml" />            </ResourceDictionary.MergedDictionaries>        </ResourceDictionary>    </UserControl.Resources>    <Grid>        <Grid.RowDefinitions>            <RowDefinition Height="Auto"/>            <RowDefinition Height="*"/>        </Grid.RowDefinitions>        <Grid Margin=" 15 10" >            <Grid.ColumnDefinitions>                <ColumnDefinition Width="Auto" />                <ColumnDefinition Width="*" />            </Grid.ColumnDefinitions>            <TextBlock Text="{bg:StringResource 用户管理}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>            <!-- 操作面板 -->            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">                <Button Content="{bg:StringResource 新增用户}" Command="{Binding AddOrUpdateUserCommand}" CommandParameter="{x:Static bg:ValueBoxes.TrueBox}" Margin="0"/>                <Button Content="{bg:StringResource 修改用户}" Command="{Binding AddOrUpdateUserCommand}" CommandParameter="{x:Static bg:ValueBoxes.FalseBox}" Margin="15 0 0 0"/>                <Button Content="{bg:StringResource 删除用户}" Command="{Binding DeleteUserCommand}" Margin="15 0 0 0"/>            </StackPanel>        </Grid>        <!-- 用户列表 -->        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">            <DataGrid.Columns>                <DataGridTextColumn Header="{bg:StringResource 用户ID}" Binding="{Binding Id}"/>                <DataGridTextColumn Header="{bg:StringResource 用户名}" Binding="{Binding UserName}"/>                <DataGridCheckBoxColumn Header="{bg:StringResource 已启用}" Binding="{Binding IsActive}"/>                <DataGridCheckBoxColumn Header="{bg:StringResource 在线}" Binding="{Binding IsOnline}"/>                <DataGridTextColumn Header="{bg:StringResource 权限}" Binding="{Binding Authority}"/>                <DataGridTextColumn Header="{bg:StringResource 创建时间}" Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}"/>            </DataGrid.Columns>        </DataGrid>    </Grid></UserControl>