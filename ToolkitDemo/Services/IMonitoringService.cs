using BgCommon.Core.Models;using ToolkitDemo.Models;namespace ToolKitDemo.Services;/// <summary>/// 定义了一个后台监控服务的标准接口./// 该接口提供了启动、停止以及动态调整监控目标等核心功能，用于实时跟踪和报告应用程序的各项状态./// </summary>public interface IMonitoringService{    /// <summary>    /// Gets 当前监控的对象.    /// </summary>    MonitoringTarget CurrentTargets { get; }    /// <summary>    /// Gets 获取一个包含所有实时监控数据的对象实例.    /// 此属性提供了对应用程序性能、设备状态和运行状态等所有监控指标的集中访问点.    /// </summary>    SystemRealTimeStatus SystemStatus { get; }    /// <summary>    /// 异步刷新设备列表.    /// </summary>    /// <returns>一个表示异步操作结果的任务.</returns>    Task RefreshDeviceListAsync();    /// <summary>    /// 异步启动监控服务并初始化后台工作线程.    /// 调用此方法后，后台线程将进入准备就绪状态，但通常处于休眠中，直到通过 <see cref="StartMonitoringAsync"/> 指定了具体的监控目标.    /// </summary>    /// <returns>一个表示异步操作结果的任务，其中包含操作是否成功的信息.</returns>    Task<OperateResult> StartAsync();    /// <summary>    /// 异步停止监控服务并安全地释放所有相关资源.    /// 此方法会向后台线程发送取消信号，并等待其优雅地终止，以确保资源的正确清理.    /// </summary>    /// <returns>一个表示异步操作结果的任务，其中包含操作是否成功的信息.</returns>    Task<OperateResult> StopAsync();    /// <summary>    /// 激活对一个或多个特定目标的实时监控.    /// 此方法通过更新监控标志位来唤醒后台线程（如果其处于休眠状态），并指示它开始收集指定目标的数据.    /// </summary>    /// <param name="target">要开始监控的目标.可以使用位或运算（|）组合多个目标.</param>    /// <returns>一个表示异步操作结果的任务，其中包含操作是否成功的信息.</returns>    Task<OperateResult> StartMonitoringAsync(MonitoringTarget target);    /// <summary>    /// 停止对一个或多个特定目标的实时监控，而不会停止整个服务.    /// 此方法会从监控标志位中移除指定目标.如果所有监控目标均被移除，后台线程将自动返回休眠状态.    /// </summary>    /// <param name="target">要停止监控的目标.可以使用位或运算（|）组合多个目标.</param>    /// <returns>一个表示异步操作结果的任务，其中包含操作是否成功的信息.</returns>    Task<OperateResult> StopMonitoringAsync(MonitoringTarget target);}